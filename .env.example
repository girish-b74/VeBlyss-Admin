# Neon/Vercel integration normally creates DATABASE_URL or POSTGRES_URL.
# Keep the exact variable names created by Vercel; the API accepts either.
DATABASE_URL=

# Optional. If omitted, the built-in temporary credentials are used for testing.
ADMIN_USERNAME=veblyss-admin
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
