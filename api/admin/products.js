const {init,sql,verify,body}=require("../_lib");function auth(req){const m=(req.headers.cookie||"").match(/(?:^|;\s*)vb_admin=([^;]+)/);return m&&verify(m[1])}
module.exports=async(req,res)=>{try{if(!auth(req))return res.status(401).json({error:"Unauthorized"});await init();if(req.method==="GET"){return res.json(await sql`SELECT * FROM products ORDER BY created_at DESC`)}if(req.method==="POST"){const b=await body(req);if(!b.id||!b.name)return res.status(400).json({error:"id and name required"});const r=await sql`INSERT INTO products(id,name,category,image,short_description,description,mrp,published,features,specifications,updated_at) VALUES(${b.id},${b.name},${b.category||""},${b.image||""},${b.short_description||""},${b.description||""},${Number(b.mrp)||0},${b.published!==false},${JSON.stringify(b.features||[])},${JSON.stringify(b.specifications||{})},now()) RETURNING *`;return res.status(201).json(r[0])}return res.status(405).json({error:"Method not allowed"})}catch(e){res.status(500).json({error:e.message})}};